import AccountRepository from './account';
import TransactionRepository from './transaction';

const repositories = [AccountRepository, TransactionRepository];

export { AccountRepository, TransactionRepository, repositories };
